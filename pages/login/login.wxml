<view class="container">
  <view class="card">
    <input placeholder="用户名" value="{{username}}" bindinput="onInput" data-field="username" />
    <input placeholder="密码" password value="{{password}}" bindinput="onInput" data-field="password" />
    <button class="btn-primary" bindtap="onLogin">登录</button>
  </view>
</view>

